# Production / Render — activate with SPRING_PROFILES_ACTIVE=prod
# Secrets: use environment variables or Render env groups, not committed files.

server.error.include-stacktrace=never
logging.level.root=WARN
