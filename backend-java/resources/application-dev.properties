# Local development (default when SPRING_PROFILES_ACTIVE=dev or unset).
# Shared defaults remain in application.properties.

# Local Mongo often has older index names (Spring vs Mongo defaults). Disabling auto-creation avoids
# startup failures; indexes still exist. Set true temporarily on a fresh DB if you need @Indexed applied.
spring.data.mongodb.auto-index-creation=false
